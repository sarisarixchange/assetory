Options -MultiViews
RewriteEngine On
RewriteBase /

# If the request is not for a real file or folder,
# send it to index.html for SPA routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]

{
  "name": "website-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview", 
    // "deploy": "npm run build && gh-pages -d dist"
    "deploy-old": "REPO=sari-sari-website npm run build && gh-pages -d dist -b gh-pages -r https://github.com/your-username/sari-sari-website.git",
    "deploy-new": "REPO=sari-sari-website-tests npm run build && gh-pages -d dist -b gh-pages -r https://github.com/your-username/new-repo-name.git"
  

  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.11",
    "vite-plugin-vue-devtools": "^7.7.0"
  }
}
